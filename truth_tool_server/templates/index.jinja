<!doctype html>

<html lang="en" xmlns="http://www.w3.org/1999/html">
<head>
    <meta charset="utf-8">

    <title>Truth Tool</title>
    <meta name="description" content="A Truthing Tool">
    <meta name="author" content="Andrew Fiorillo">

    <link rel="stylesheet" href="{{ url_for('static', filename='css/dh.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/normalize.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/skeleton.css') }}">

    <!--[if lt IE 9]>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.3/html5shiv.js"></script>
    <![endif]-->

    <!-- Favicon
    –––––––––––––––––––––––––––––––––––––––––––––––––– -->
    <link rel="icon" type="image/png" href="images/favicon.png">

</head>

<body>


    <!-- Primary Page Layout
    –––––––––––––––––––––––––––––––––––––––––––––––––– -->
    <div class="container">
    <div class="row">
        <div class="one-half column" style="margin-top: 25%">
            <h2 class="unselectable">Is This Review Text Profane?</h2>
            <div class="invisible" id="review-id">{{reviewId}}</div>

            <blockquote>
                {{reviewText}}
                <cite>{{reviewUser}}</cite>
            </blockquote>

            <div id="radio-options">
                <input type="radio" class="radio-options" id="radio-profane" value="True"> <span class="unselectable">Profane</span>
                <input type="radio" class="radio-options" id="radio-not-profane" value="False"> <span class="unselectable">Not Profane</span>
            </div>

            <button id="submit-button">Submit</button>
        </div>
    </div>

    <div class="row">
        <div id="snackbar" class="invisible">You must select a radio option...</div>
    </div>
    </div>


    <script src="{{url_for('static', filename='dist/jquery-3.3.1.min.js')}}" type="text/javascript"></script>
    <script src="{{url_for('static', filename='dist/bundle.js')}}" type="text/javascript"></script>
</body>
</html>